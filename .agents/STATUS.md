# 当前状态

最后更新：2026-06-11

## 仓库状态

- 主仓库：`policy_manus`
- 当前分支：`main`，已与 `origin/main` 的 `e250705` 同步。
- 当前工作区存在尚未提交的 Docker 构建与迁移配置修复。
- 不应在这些改动提交或暂存前执行会覆盖工作区的操作。

## 已完成

> 细节以 `git log` 为准，本节只记里程碑。

- 多租户后端闭环：tenants/users/memberships 表 + JWT 全流程 + 租户切换。
- 租户隔离：sessions、files、平台配置（platform admin 保护）。
- Docker 资源改为 `policy-*`，默认库名 `policy_manus`。
- 前端原型入 `frontend-prototype/`，正式前端仍在 `ui/`。
- `.agents/` 协作记忆体系落地。
- 前端认证闭环：登录/注册页、Token 存储与 401 自动刷新、Authorization 注入、路由守卫、租户切换器、登出。
- 平台管理员可从侧边栏进入模型 API 配置，安全更新 OpenAI 兼容地址、密钥和模型参数。
- 完成 ARM64 开发机上的全量 Docker Compose 构建与启动验证。
- Alembic 迁移连接改为读取统一运行时数据库配置。

## 未完成

- 前端认证闭环的真机联调（尚未对运行中的后端实测）。
- 成员邀请和组织成员管理。
- 多租户自动化测试。
- RAG 数据模型和向量存储。
- 文档解析、分块和 Embedding。
- 知识库页面和检索引用展示。
- 政策爬取。
- 报告生成流水线。
- GitHub Actions 和分支保护。

## 当前最高优先级

1. 对前端认证闭环 + 后端多租户做端到端真机联调。
2. 推进 RAG 数据模型与向量存储。
3. 尽快打通一份政策文件的上传、索引、检索和引用回答。
4. 建立 `develop`、Issue、PR 和 CI 工作流。

## 已知风险

- 后端多租户测试覆盖不足，跨租户读取风险尚未系统验证（计划在前端认证页就绪后端到端一起测）。
- 前端认证闭环仅通过类型检查与构建，尚未对运行中的后端做真机联调。
- `.env` 和 `api/config.yaml` 是 Docker 启动前置条件。
- 本地 `.env` 和 `api/config.yaml` 仍使用 COS/LLM 占位配置；模型密钥可在网页中补充，文件上传和 Agent 调用尚未验证。
- 十天范围非常紧，任何新增基础设施都必须证明能直接服务主链路。

## 更新规则

只记录最新事实。任务细节放 GitHub Issue，临时交接放 `handoffs/`，架构原因放 `decisions/`。
