# PolicyManus 项目基线

Issue：待创建
分支：当前为 `main`
负责人：项目组
更新时间：2026-06-09

## 目标

为十天冲刺建立两人和 Agent 都能读取的共享上下文。

## 已完成

- 建立 `AGENTS.md` 统一入口。
- 建立产品、架构、API、数据模型、状态和运行手册。
- 记录 pgvector 和 Token 租户上下文两项决策。
- 明确 Yuxi 只作为知识库设计参考，不整体移植。

## 接口与迁移

本任务没有修改运行时代码、API 或数据库迁移。

## 验证

需要确认所有新增 Markdown 文件已被 Git 跟踪，且没有覆盖现有未提交修改。

## 未完成

- 当前品牌及 Docker 隔离改动尚未提交。
- GitHub Issues、Project、CI、`develop` 分支和分支保护尚未建立。
- 两名开发者需要确定各自负责的首个 Issue。

## 风险

- 当前工作区已有大量未提交修改，创建功能分支前应先单独提交。
- `STATUS.md` 必须保持简短，否则 Agent 每次读取成本会持续上升。

## 下一步

1. Review 并提交当前品牌重命名和共享记忆。
2. 创建 `develop` 分支及首批 Issues。
3. 一人开始前端认证，一人开始 RAG 数据模型和租户测试。

