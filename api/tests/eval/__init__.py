"""核心能力效果评测（带标注数据集 + 评测指标 + 可复现门禁）。

与 `tests/app`（功能正确性单测）不同，本目录衡量的是 AI 能力的**效果好坏**：
给定带 golden 标注的数据集，对核心纯函数/管线打分，输出 准确率/精确率/召回率/recall@k/MRR
等可量化指标，并以"指标≥阈值"的方式作为可复现门禁（CI 可跑、离线确定）。

设计原则——**离线确定性可复现**：
- ⑥资质差距、③政策匹配走纯函数，天然确定；
- ⑤截止抽取、RAG 检索的真实 LLM/Embedding 调用只在 `--live` 重录时发生，
  平时评测跑**冻结的输出/向量快照**（committed fixtures），保证任何人 clone 后
  一条命令复现同一组数字。

数据集见 `datasets/`，指标纯函数见 `metrics.py`，复现入口见 `scripts/run_eval.py`。
"""
